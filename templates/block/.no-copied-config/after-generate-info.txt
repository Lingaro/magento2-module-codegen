1. Add block in your layout xml file:

    <block class="{{ vendorName }}\{{ moduleName }}\Block\{{ blockName }}"
           name="{{ vendorName|lower }}.{{ moduleName|lower }}.{{ blockName|lower }}"
           template="{{ vendorName }}_{{ moduleName }}::{{ blockName|lower }}.phtml"/>