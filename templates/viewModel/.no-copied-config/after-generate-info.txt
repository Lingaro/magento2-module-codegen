1. Add block in your layout xml file:

    <block name="{{ vendorName|lower }}.{{ moduleName|lower }}.{{ viewModelName|lower }}" template="{{ vendorName }}_{{ moduleName }}::{{ viewModelName|lower }}.phtml">
        <arguments>
            <argument name="view_model" xsi:type="object">{{ vendorName }}\{{ moduleName }}\ViewModel\{{ viewModelName }}</argument>
        </arguments>
    </block>